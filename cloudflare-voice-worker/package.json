{
  "name": "boston-builders-voice-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AI voice calling WebSocket bridge",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
